spring:
  application:
    name: todoapp
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:todo-app}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:postgres}